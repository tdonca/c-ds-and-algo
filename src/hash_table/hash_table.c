
// TODO: Remove explicit path dependency when switching to CMake
#include "include/hash_table.h"

// USE UTHASH library and just have this be a wrapper implementation
// try to make it a little bit generic
// ex: use HASH_ADD() instead of the specific HASH_ADD_INT()

// Goal: handle ints, strings, and chars seamlessly


// Followup will be implementing the hash table from scratch 